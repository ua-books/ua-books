default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: ua_books

test:
  <<: *default
  database: ua_books_test

production:
  <<: *default
  database: ua_books
